---
title: "强制使用现有打包脚本"
usage_scenario:
    - "需要构建APK时执行正确的打包命令"
    - "排查APK体积异常时检查打包流程"
keywords:
    - "build_pack.ps1"
    - "打包规范"
    - "APK构建"
---

项目打包必须使用现有的 `build_pack.ps1` 脚本（支持 large/medium/mini 三档位），严禁自行编写或修改打包逻辑
