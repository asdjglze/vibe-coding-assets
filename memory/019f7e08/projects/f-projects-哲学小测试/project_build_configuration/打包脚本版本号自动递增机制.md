---
title: "打包脚本版本号自动递增机制"
usage_scenario:
    - "执行多档位APK打包前确认版本策略"
    - "排查重复安装包冲突或验证构建产物唯一性"
keywords:
    - "版本号递增"
    - "build_pack"
    - "versionCode"
    - "versionName"
---

打包脚本（build_pack.ps1）必须实现版本号自动递增机制：每次运行脚本时 `versionCode` 自动 +1，`versionName` 同步更新为 `x.y.<code>` 格式，确保每次构建产物具有唯一版本号。
