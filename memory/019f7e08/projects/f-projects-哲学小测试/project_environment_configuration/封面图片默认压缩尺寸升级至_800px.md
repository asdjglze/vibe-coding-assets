---
title: "封面图片默认压缩尺寸升级至 800px"
usage_scenario:
    - "调整封面加载尺寸需求时参考当前 800px 策略"
    - "排查封面加载卡顿或清晰度问题时检查默认参数"
keywords:
    - "封面尺寸"
    - "800px"
    - "缩略图"
    - "性能优化"
---

项目封面加载策略升级：后端 `/api/cover/<id>` 接口默认返回 800px 缩略图而非原图；前端优先使用 `coverUrl800` 字段，兼容旧字段 `coverUrl300`，缺失时自动补 `size=800` 参数兜底
