---
title: "翻页与滚动模式渲染一致性排查修复"
usage_scenario:
    - "遇到不同阅读模式（翻页/滚动）显示效果不一致时进行排查"
    - "修复跨模式渲染差异，确保排版参数统一读取配置"
    - "检查 Quote/Signature/Poetic 等特定块的字号行距计算逻辑"
keywords:
    - "渲染一致性"
    - "翻页模式"
    - "排版参数"
    - "字号计算"
---

## 任务描述
排查并修复翻页模式与滚动模式在排版参数（行距/段距/缩进）及具体文本块（Quote/Signature/Poetic 等）渲染上的不一致问题。

## 执行过程
```mermaid
graph TD
    A[用户反馈:翻页模式渲染乱搞] --> B[确认配置读取源:ReaderSettingsStore]
    B --> C[对比两模式配置读取逻辑:lineHeightRatio/paragraphSpacingSp/indentEm]
    C --> D{配置是否一致？}
    D -- 是 --> E[逐项核对具体块渲染:MdBody vs PagedReaderBodies]
    E --> F[发现差异点1:Poetic字距随缩放变化]
    E --> G[发现差异点2:Signature落款字号计算]
    F --> H[修复Poetic letterSpacingSp不乘scale]
    G --> I[修正Signature fontSize为bodySp-1f]
    H --> J[编译验证通过]
    I --> J
```

## 任务总结
确认两模式均正确读取 settings 配置。修复两处真实差异：1. Poetic 诗块字距不再随行宽缩放 (letterSpacingSp * scale -> letterSpacingSp)；2. Signature 落款字号统一为正文小一号 (bodySp - 1f)。期间曾误判落款字号方向，需警惕此类反向修改。
