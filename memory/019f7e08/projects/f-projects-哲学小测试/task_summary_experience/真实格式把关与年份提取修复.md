---
title: "真实格式把关与年份提取修复"
usage_scenario:
    - "文件入库前需要校验真实格式并修复后缀的场景"
    - "处理多层后缀文件（如 .rar.zip.pdf）的解压与命名逻辑"
    - "从文件名或元数据中提取发布年份需避免误判的场景"
keywords:
    - "真实格式把关"
    - "后缀修复"
    - "年份提取"
    - "魔数识别"
---

## 任务描述
1. 实现"按需真实格式把关"：在文件进入处理流程前，通过魔数识别真实类型，修复多层后缀（如 .rar.zip.pdf），当场解压真压缩包，确保归档使用正确后缀。
2. 修复年份提取 Bug：解决标准号中段数字（如 GB/T 19264.2）被误识别为发布年份的问题，确立"AI/官方数据优先，程序严格形兜底"的策略。

## 执行过程
```mermaid
graph TD
    A[需求:真实格式把关与年份修复] --> B[定位 unpacker.py 与 standard.py]
    B --> C[新增 prepare_input_file 与 unpack_one 函数]
    C --> D[接入 standard.py 主循环与 agent.py 入口]
    D --> E[新增 _book_stem 链剥常用后缀]
    E --> F[编写 mock 测试用例验证伪装文件处理]
    F --> G[修复静态检查发现的未定义变量 bug]
    G --> H[定位 _extract_year 正则过于宽泛问题]
    H --> I[修改正则仅匹配编号尾年或孤立四位年]
    I --> J[引入 _ai_year_of 辅助函数提升优先级]
    J --> K[更新 meta 构建逻辑：AI > 接口 > 程序兜底]
```

## 任务总结
成功实现按需格式把关：`prepare_input_file` 在文件处理前验身，魔数识别真类型，修复多层后缀，真压缩包就地解压并备份原件；书名兜底改用 `_book_stem` 链剥常规后缀。修复年份提取 Bug：`_extract_year` 改为严格匹配 `-2013` 形式或孤立四位年，避免从标准号中段误取年份；元数据构建中年份优先级调整为 AI 输出 > 接口数据 > 文件名兜底。
