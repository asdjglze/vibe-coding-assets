---
title: "头像资源整理与非1:1图片替换"
usage_scenario:
    - "Android项目中批量替换头像资源并统一命名规范"
    - "处理数据库资源名与文件系统不一致的问题"
    - "排查因资源文件名特殊字符导致的编译失败"
keywords:
    - "头像资源"
    - "非1:1裁剪"
    - "资源命名规范"
---

## 任务描述
整理项目头像资源：将非1:1比例的旧头像文件删除，用对应的(1)副本（1:1新图）替换，并确保文件名符合Android资源命名规范。

## 执行过程
```mermaid
graph TD
    A[需求:头像资源整理] --> B[查询DB确认avatarRes字段及存储格式]
    B --> C[发现avatarRes存的是avatar_xxx格式]
    C --> D[编写脚本扫描drawable-nodpi目录检查所有头像尺寸]
    D --> E[识别规律:(1)后缀的副本均为1:1新图，原文件为非1:1]
    E --> F[编写整理脚本:删除旧非1:1文件 + 重命名(1)副本为正确资源名]
    F --> G[执行整理并校验:剩余非1:1数量/残留副本/DB一致性]
    G --> H[编译验证发现QuoteApplication.kt作用域错误]
    H --> I[修复this作用域后重新编译通过]
```

## 任务总结
成功清理13张非1:1头像，将(1)副本重命名为avatar_xxx格式，修复了QuoteApplication.kt中CoroutineScope作用域导致的编译错误，确保资源与DB一致且编译通过。
