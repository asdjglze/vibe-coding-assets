---
title: "图书管理系统三阶段流程重构与清理规则定案"
usage_scenario:
    - "重构文件处理流水线或引入多阶段处理架构时参考流程设计"
    - "配置自动化清理规则（如删除特定扩展名文件、报告可疑文件）"
    - "设计异常处理机制（如待人工文件夹、缺失记录库）以优化 AI 调用效率"
keywords:
    - "三阶段流程"
    - "粗扫描清理"
    - "待人工机制"
    - "缺失记录库"
---

## 任务描述
重构图书管理系统处理流程为三阶段（粗扫描→快通道发布→精修），明确粗扫描清理规则、待人工机制及按需判定逻辑。

## 执行过程
```mermaid
graph TD
    A[需求:确立三阶段流程] --> B[调研现有扫描与清理代码]
    B --> C[定义粗扫描清理清单：删.url/.lnk/Mac垃圾/autorun.inf/*.tmp*.bak; 报告可执行脚本]
    C --> D[设计待人工机制：四区结构 + 细分标签 + 锁语义]
    D --> E[设计缺失记录库：AI 信号触发程序写入专用库]
    E --> F[确认按需判定逻辑：程序比对标签/字段/缺失库，非每本问 AI]
    F --> G[更新规划文档 v1.2：整合所有新规则]
```

## 任务总结
成功制定 v1.2 版规划：
1. **粗扫描**：新增散落 .url/.lnk、Mac 垃圾、autorun.inf、*.tmp/*.bak 自动删除；可执行脚本只报告不删除。
2. **待人工**：书库根新增第四区，挂「待分类/待富化/待校对」细分标签，进入即锁定跳过自动流程。
3. **缺失记录库**：程序侧专用库，记录 AI 无法找到的元数据，避免重复搜索和误挂标签。
4. **按需判定**：通过程序比对标签、字段值、缺失库决定小阶段，无需全量调用 AI。
