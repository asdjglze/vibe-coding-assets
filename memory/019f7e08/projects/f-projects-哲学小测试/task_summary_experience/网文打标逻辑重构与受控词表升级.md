---
title: "网文打标逻辑重构与受控词表升级"
usage_scenario:
    - "重构内容分类或打标系统，从启发式规则转向AI信号驱动"
    - "为打标系统增加受控词表（Taxonomy）及程序侧清洗过滤逻辑"
    - "处理多源数据时的标签一致性与冲突校验"
keywords:
    - "网文打标"
    - "受控词表"
    - "一致性校验"
    - "AI信号"
    - "标签清洗"
---

## 任务描述
重构网文打标逻辑：废除按 .txt 后缀猜网文的规则，改为由识别 AI 在 final 阶段输出 webnovel 信号；升级受控词表增加“首发平台”维度，并实现程序侧的标签清洗与一致性校验。

## 执行过程
```mermaid
graph TD
    A[需求:废除.txt判据,改用AI信号+受控词表] --> B[分析现有代码:organizer/tagger/agent]
    B --> C[修改agent_system.md:final schema增加webnovel字段]
    C --> D[修改tagger.py:新增webnovel_generate_tags函数]
    D --> E[升级webnovel_taxonomy.json:增加平台维度+男频同人]
    E --> F[实现_clean_webnovel_tags:过滤自创词+平台频道一致性校验]
    F --> G[修改organizer.py:按meta.webnovel信号分流打标]
    G --> H[清理旧逻辑:删除tagger.run中的.txt分支及补标脚本误判规则]
    H --> I[全量语法验证与清洗用例测试]
```

## 任务总结
1. **判定前移**：识别 Agent 根据正文证据输出 `webnovel` 布尔值，不再依赖文件后缀。
2. **受控打标**：网文打标引入 `webnovel_generate_tags`，将频道、平台、大类、流派词表喂给 AI，程序侧通过 `_clean_webnovel_tags` 进行双保险过滤。
3. **词表升级**：`webnovel_taxonomy.json` v2.0 增加“首发平台”枚举（起点/晋江/飞卢等），男频大类增加“同人”。
4. **一致性校验**：清洗逻辑增加平台与频道的一致性检查（如女频+起点则丢弃平台标签），防止 AI 逻辑冲突。
5. **代码清理**：彻底移除 `organizer.py` 和 `tagger.py` 中基于 `.txt` 后缀的硬编码分支。
