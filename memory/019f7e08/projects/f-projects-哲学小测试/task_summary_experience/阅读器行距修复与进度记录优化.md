---
title: "阅读器行距修复与进度记录优化"
usage_scenario:
    - "排查 Compose 阅读器行距异常或样式继承问题"
    - "实现跨模式（滚动/翻页）统一的文本排版逻辑"
    - "优化移动端阅读进度记录的灵敏度和可靠性"
keywords:
    - "Compose"
    - "行距修复"
    - "进度记录"
    - "LocalTextStyle"
---

## 任务描述
修复 Android Compose 阅读器中翻页模式与滚动模式行距不一致问题，并优化滚动模式下的阅读进度记录机制。

## 执行过程
```mermaid
graph TD
    A[需求:修复翻页模式行距异常] --> B[定位 PagedReaderBodies.kt 和 ReaderScreen.kt]
    B --> C[分析根因:LocalTextStyle 被显式 style 参数覆盖导致行距丢失]
    C --> D[修改 PagedReaderBodies.kt:6处显式设置 lineHeight=字号×lineHeightRatio]
    D --> E[修改 MdBody.kt 和 ReaderScreen.kt:同步滚动端显式行高]
    E --> F[编译验证通过]
    F --> G[需求:优化滚动模式进度记录灵敏度]
    G --> H[修改 ReaderScreen.kt:debounce 从 600ms 降至 200ms]
    H --> I[添加 DisposableEffect 兜底逻辑防止快速切换时丢失进度]
    I --> J[修改 ReaderViewModel.kt:更新 resumePercent 范围至 100]
    J --> K[再次编译验证通过]
```

## 任务总结
成功解决翻页模式行距偏紧问题：识别出 Compose Text 显式传 style 会替换 LocalTextStyle 导致行距退化为自然行高的根因，在 PagedReaderBodies.kt 和 ReaderScreen.kt/MdBody.kt 中统一显式设置 lineHeight = fontSize * lineHeightRatio。同时优化进度记录：将 debounce 时间从 600ms 减至 200ms，并新增 DisposableEffect 兜底逻辑确保快速切换时也能记录最终位置，支持 100% 进度语义。
