---
title: "每日内容人物统一与回退逻辑移除"
usage_scenario:
    - "实现基于特定实体（如用户、角色）的内容推荐逻辑"
    - "需要严格保证数据归属一致性，禁止混合来源"
    - "处理实体缺失内容时的降级显示策略"
keywords:
    - "DailyRepository"
    - "人物统一"
    - "回退逻辑"
    - "内容推荐"
---

## 任务描述
修正每日内容推荐逻辑，严格执行「先确定性随机一位人物，再仅从该人物名下取内容」的原则。若该人物没有某类内容（语录/诗词/文章），则该类卡片直接不显示，严禁回退到其他人物选取。

## 执行过程
```mermaid
graph TD
    A[需求:统一每日人物逻辑] --> B[分析 DailyRepository 现有代码]
    B --> C[发现旧逻辑存在回退机制]
    C --> D[重构 todayContentOf 方法]
    D --> E[移除 pickPersonWith 回退辅助函数]
    E --> F[修改逻辑: 今日人物无内容直接返回 null]
    F --> G[验证 HomeScreen 对 null 的处理（整卡不渲染）]
```

## 任务总结
1. 重构 `DailyRepository.todayContentOf`，确保人物与内容强绑定。
2. 删除了允许跨人物回退的 `pickPersonWith` 逻辑。
3. 实现了当今日人物缺少特定类型内容时，对应卡片完全隐藏的行为。
