---
title: "协同工作规程新增发展扬弃及文档格式定义"
usage_scenario:
    - "大型AI协作规则或系统提示词的重构与版本管理"
    - "需要严格保证配置一致性的大型文件修改流程"
    - "涉及多轮迭代和精确替换的规则文件维护"
keywords:
    - "规则重构"
    - "文档格式定义"
    - "哈希校验"
    - "发展扬弃"
---

## 任务描述
为协同工作规程新增“发展扬弃”认识论模块，补充完整的落盘文档格式定义（含7类文档模板），并根据用户反馈进行多轮迭代修订。

## 执行过程
```mermaid
graph TD
    A[需求:新增发展扬弃模块] --> B[编写新模块内容及总表/流程图更新]
    B --> C[写入暂存文件_tmp_rule.md]
    C --> D[复制至目标路径并执行哈希校验]
    D --> E[需求:补充落盘文档格式定义]
    E --> F[整合7类文档模板及目录结构]
    F --> G[再次写入暂存并校验]
    G --> H[接收5点修订指令]
    H --> I[反向复制目标文件至暂存]
    I --> J[执行SearchReplace精准修改:总纲/存放层级/模板引用]
    J --> K[回写目标文件并二次校验]
```

## 任务总结
成功将规则文件扩展至12模块完整版。关键变更包括：
1. **新增模块十一**：确立过去不可变、现在必须变、变化分级等原则。
2. **完善文档体系**：定义了docs/下的7类目录结构及详细Markdown模板。
3. **优化存储策略**：区分项目级与全局级（~/.qoder/docs/）文档存放路径。
4. **流程标准化**：确立了“暂存编辑->复制落盘->哈希校验”的防篡改发布流程。
