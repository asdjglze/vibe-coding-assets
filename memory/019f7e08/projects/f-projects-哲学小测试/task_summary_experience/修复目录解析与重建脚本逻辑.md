---
title: "修复目录解析与重建脚本逻辑"
usage_scenario:
    - "修复网页爬虫解析逻辑失败的问题"
    - "优化Python脚本中的正则表达式提取逻辑"
    - "排查和处理数据库并发锁导致的数据不一致"
keywords:
    - "目录解析"
    - "译本提取"
    - "数据库锁"
    - "脚本修复"
---

## 任务描述
修复并运行 `rebuild_catalog.py` 脚本，解决目录解析失败、译本标题误匹配、空书残留及数据库并发锁等问题。

## 执行过程
```mermaid
graph TD
    A[需求:修复脚本并查看输出] --> B[定位 fetch_tocs.py 解析 Bug]
    B --> C[修复 book_prefix 计算逻辑以正确匹配文章链接]
    C --> D[在 rebuild_catalog.py 中增加实时日志 log()]
    D --> E[修复 plan 5元组解包错误]
    E --> F[优化 extract_translator 增加白名单过滤垃圾译者名]
    F --> G[增加删除空书逻辑清理无效目录]
    G --> H[解决多进程并发导致的数据库锁问题]
```

## 任务总结
成功修复目录解析逻辑，使 `tocs.json` 条目从 0 增至 10911；优化译本识别逻辑，消除误匹配的“另一译本”干扰；通过增加日志和清理空书逻辑，提升了重建过程的透明度和数据质量。
