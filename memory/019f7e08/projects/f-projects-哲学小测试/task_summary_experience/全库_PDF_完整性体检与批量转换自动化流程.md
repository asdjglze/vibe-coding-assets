---
title: "全库 PDF 完整性体检与批量转换自动化流程"
usage_scenario:
    - "大规模电子书库的完整性检查与质量评估"
    - "SWF 源文件批量转 PDF 的自动化脚本编写与调试"
    - "排查 PDF 生成过程中的缺页/损坏/WAF 验证页问题"
keywords:
    - "PDF 体检"
    - "批量转换"
    - "WAF 验证页"
    - "断点续传"
---

## 任务描述
对全库电子书进行完整性体检与批量转换：先盘点所有 PDF 并生成清单，再编写自动化工具逐页检测空页、坏页及疑似 WAF 验证页（尺寸异常），最后将 SWF 源文件批量转换为 PDF，并处理转换中的超时与坏文件问题。

## 执行过程
```mermaid
graph TD
    A[需求:确认 PDF 完整性] --> B[编写_pdf_stock.py 盘点全库 PDF]
    B --> C[生成清单_json 并输出目录结构]
    C --> D[编写_pdfcheck.py 体检工具]
    D --> E{迭代优化检测维度}
    E -->|初版 | F[检测空页/坏页]
    F --> G[发现漏检 WAF 验证页]
    G --> H[增加页图尺寸异常检测逻辑]
    H --> I[启动体检后台运行]
    I --> J[编写_fix_csv.py 补全缺失 iframe.csv]
    J --> K[编写_batch_swf2pdf.py 批量转换]
    K --> L[发现周恩来外交文选页数异常]
    L --> M[诊断_log 定位渲染超时与坏文件]
```

## 任务总结
成功实现全库 PDF 自动化管理：
1. **盘点**：识别全库 183 个 PDF，生成结构化清单。
2. **体检**：开发多维度体检脚本，通过低分辨率渲染查空页、读取异常查坏页、宽度偏离中位数查 WAF 假页，支持有源目录书的页数比对。
3. **转换**：批量串行转换 SWF 为 PDF，具备断点续传、坏文件自动剔除功能；针对转换超时问题，通过日志分析定位到 Playwright 连接拒绝与 Timeout 原因，为后续修复提供依据。
4. **修补**：自动补全被校验误拦的 2 字节系统文件 iframe.csv。
