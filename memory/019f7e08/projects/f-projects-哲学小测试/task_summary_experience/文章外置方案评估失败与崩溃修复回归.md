---
title: "文章外置方案评估失败与崩溃修复回归"
usage_scenario:
    - "评估将大文本内容从数据库移至文件系统的需求"
    - "排查Room数据库Schema不匹配导致的崩溃问题"
    - "处理涉及全文搜索功能的数据库结构变更"
keywords:
    - "文章外置"
    - "Room Migration"
    - "崩溃修复"
    - "搜索重构"
---

## 任务描述
尝试将文章正文外置为 md/txt 文件以提升性能，后评估复杂度放弃，转回修复 App 崩溃问题。

## 执行过程
```mermaid
graph TD
    A[需求:文章外置存储提升性能] --> B[分析App端读取逻辑]
    B --> C[发现UI层强依赖article.content字段]
    C --> D[评估改动范围: DAO/Repository/搜索全链路]
    D --> E[识别高风险点: 搜索SQL LIKE content失效]
    E --> F[决定放弃外置方案,回归崩溃修复]
    F --> G[定位崩溃原因: DB Schema v9/v10不匹配]
    G --> H[修改Article.kt添加author字段]
    H --> I[升级DB Version并添加Migration_9_10]
    I --> J[在QuoteApplication注册Migration]
```

## 任务总结
1. **架构决策**：放弃文章外置方案，因涉及搜索逻辑重构（原SQL依赖content列）及大量代码改动，风险过高。
2. **崩溃修复**：通过添加 `author` 字段、升级 Room 数据库版本至 v10 并注册 `MIGRATION_9_10` 解决 Schema 不匹配导致的重建崩溃问题。
