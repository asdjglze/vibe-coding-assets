---
title: "图书分类Agent自主导航与猜测禁令重构"
usage_scenario:
    - "重构基于层级树的AI决策逻辑，避免死循环或错误分支硬凑"
    - "为AI系统添加目录查询工具以增强全局视野和决策准确性"
    - "处理分类系统中‘走错路’后的回退与人工干预机制"
keywords:
    - "自主导航"
    - "猜测禁令"
    - "list_tree工具"
    - "分类逻辑重构"
---

## 任务描述
重构图书自动分类Agent的逻辑，解决AI在错误分类路径下无法回退、强行钻入无效层级、以及缺乏目录全局视野的问题。

## 执行过程
```mermaid
graph TD
    A[问题诊断：AI选错分支后硬凑近似类号] --> B[定位根本原因：旧提示词强制下钻 + 无目录查询工具]
    B --> C[修改提示词：引入自主导航 + 猜测禁令 + 走错即停规则]
    C --> D[新增 list_tree 工具：支持递归展开官方子树，让AI先查全貌再决策]
    D --> E[调整 _MAX_STEPS: 40 -> 100 以支持充分探索]
    E --> F[优化 decide_level 逻辑：增加叶子节点自动退出判断]
    F --> G[移除强制下钻残留指令：改为尽量第四层 + 泛论停上位]
    G --> H[全链路测试验证：TU206正确路径与错误分支打回]
```

## 任务总结
成功实施分类逻辑重构：
1. **新工具**：新增 `list_tree` 工具，支持不带参数列一级大类或指定类号展开子树（deep<=3），让AI能一眼确认类号是否存在及含义。
2. **提示词升级**：废除“绝不允许停在还有子类目的层级”，改为“猜测禁令”（无证据不硬定）、“有证据先验证”（CIP号需list_tree查证）、“发现走错立即停”（错误分支直接留人工）。
3. **流程优化**：`decide_level` 增加叶子节点自动结束逻辑（无子目直接返回final），默认目标设为“尽量下钻到第四层”。
4. **结果**：实测《建筑设计资料集》可正确归入 TU206，错误路径（如TU242）被即时打回，不再出现乱选近似类号的情况。
