---
title: "批量书籍处理时按需清理预览图缓存"
usage_scenario:
    - "批量处理文档/书籍时优化存储策略，避免中间缓存堆积"
    - "实现\"处理完即清理\"的单线程串行资源释放模式"
    - "排查或修复因临时文件过多导致的磁盘空间不足问题"
keywords:
    - "预览图缓存"
    - "状态清理"
    - "批量处理"
    - "存储优化"
---

## 任务描述
优化书籍分类程序，实现"每处理完一本书即清理其预览图缓存"的逻辑，防止数百本书处理后产生数GB的无用中间数据。

## 执行过程
```mermaid
graph TD
    A[需求:入库后及时清理预览图缓存] --> B[搜索_save_images调用点定位生成位置]
    B --> C[定位recognizer.py与standard.py的主循环]
    C --> D[设计清理策略:单本串行处理，处理完即清]
    D --> E[新增_cleanup_preview_imgs函数]
    E --> F[在循环开头调用清理上一本缓存]
    F --> G[在循环末尾补清最后一本缓存]
    G --> H[编写单元测试验证清理效果]
    H --> I[实测验证目录与文件完全移除]
```

## 任务总结
成功在 recognizer.py 和 standard.py 中集成 _cleanup_preview_imgs 函数。逻辑为：每本书识别期间保留图片供AI查看，进入下一本书前清空上一本的 state/preview_imgs 目录及 std_prev_p*.png 文件。经测试，该机制确保任意时刻磁盘仅保留当前一本的图片（几MB），彻底解决批量处理时的存储堆积问题。
