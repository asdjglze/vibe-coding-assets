---
title: "人物列表按名字笔画排序功能实现"
usage_scenario:
    - "移动端/桌面端应用列表按汉字笔画排序的实现"
    - "利用外部字典数据库生成静态资源供客户端排序使用"
keywords:
    - "笔画排序"
    - "stroke_dict"
    - "人物列表"
    - "Python生成JSON"
---

## 任务描述
为 App 人物列表实现按名字笔画排序功能，并在 UI 增加说明提示。

## 执行过程
```mermaid
graph TD
    A[需求:人物列表按笔画排序] --> B[检查 Python 环境及 cnradical/pypinyin 等库]
    B --> C[定位 SQLite 新华字典数据库并验证 bihua 字段]
    C --> D[编写 _tmp_check_stroke.py 验证名字用字覆盖率]
    D --> E[编写 scripts/gen_stroke_dict.py 生成 stroke_dict.json]
    E --> F[创建 Kotlin StrokeOrderSorter 读取 JSON 进行排序]
    F --> G[修改 PersonSelectionRepository 调用排序器]
    G --> H[在 PersonSelectionScreen 添加'本排名按名字笔画数排列'提示]
    H --> I[更新 build_pack.ps1 将字典生成集成到打包流程]
```

## 任务总结
成功实现人物列表笔画排序：通过 Python 脚本从新华字典提取人物名字用字的笔画数生成 assets/stroke_dict.json；Kotlin 端实现 StrokeOrderSorter 读取该字典并按逐字笔画序列排序（同笔画按 id 升序）；UI 层增加文字提示；打包脚本自动同步字典。
