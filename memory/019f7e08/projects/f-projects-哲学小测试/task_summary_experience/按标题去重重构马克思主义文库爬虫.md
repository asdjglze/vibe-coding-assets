---
title: "按标题去重重构马克思主义文库爬虫"
usage_scenario:
    - "需要优化爬虫避免重复下载相同内容时参考去重策略"
    - "重构数据入库流程以支持多源数据（DB/主页/本地）优先级匹配"
    - "处理大量书籍目录解析与文章标题归一化逻辑"
keywords:
    - "标题去重"
    - "爬虫重构"
    - "拾遗汇编"
---

## 任务描述
重构马克思主义文库爬虫策略：放弃按文件名重复下载，改为先按【标题】对应数据库/主页数据复用，仅缺失时才下载，多余文章归入《拾遗汇编》。

## 执行过程
```mermaid
graph TD
    A[需求:避免重复下载] --> B[编写_title_match.py验证标题复用率]
    B --> C[确认复用率高后停掉旧retry_books进程]
    C --> D[创建fetch_tocs.py抓取166本书目录页解析标题+href]
    D --> E[改造rebuild_catalog.py:加载tocs.json替代原书数据]
    E --> F[实现resolve函数:DB同标题→主页同标题→书同href→才下载]
    F --> G[兜底逻辑:无目录文章按人建《XXX拾遗汇编》]
    G --> H[添加实时日志输出监控进度]
```

## 任务总结
成功实现按标题去重爬取：新增 _title_match.py 验证复用率，fetch_tocs.py 抓取目录结构，rebuild_catalog.py 改造为优先复用 DB/主页/书数据，仅缺失时带间隔下载，未归目文章自动收容至《拾遗汇编》。
