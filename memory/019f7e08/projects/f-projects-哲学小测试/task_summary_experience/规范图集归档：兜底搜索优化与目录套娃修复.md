---
title: "规范图集归档：兜底搜索优化与目录套娃修复"
usage_scenario:
    - "修复基于AI兜底的分类归档系统中的搜索质量问题"
    - "解决自动化归档中产生的目录层级重复（套娃）问题"
    - "优化标准/规范类文档的年份自动提取与归档"
keywords:
    - "规范归档"
    - "AI兜底"
    - "标准号提取"
    - "目录套娃"
---

## 任务描述
修复规范图集归档中因标准号缺失导致的AI兜底搜索失败及目录套娃问题。

## 执行过程
```mermaid
graph TD
    A[需求:修复规范归档缺陷] --> B[分析日志定位根因]
    B --> C[发现初判未提取std_number导致程序分类失败]
    C --> D[修复兜底搜索词:去除类型前缀,聚焦书名+编号]
    D --> E[增强AI提示词:强制推断并输出pub_year]
    E --> F[修改organize_standard后处理逻辑]
    F --> G[剔除AI输出中的一级目录名以防套娃]
    G --> H[回填pub_year到归档路径]
```

## 任务总结
1. **搜索优化**：在 `standard.py` 中，兜底搜索不再使用“类型+书名”（易搜出百科垃圾），改为“书名+标准+编号”。
2. **结构化增强**：提示AI在兜底时必须输出 `pub_year`（从标准号如GB 55001-2021中提取），并写入 metadata。
3. **防套娃逻辑**：在后处理中，自动剔除 AI 输出 dirs 中与一级目录重复的元素（如“国家标准”），防止生成 `1 - 国家标准\国家标准` 这种冗余路径。
4. **年份回填**：归档路径动态插入 `pub_year` 层，解决“无年份”归类问题。
