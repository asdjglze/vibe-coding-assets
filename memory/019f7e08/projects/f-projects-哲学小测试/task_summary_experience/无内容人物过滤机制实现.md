---
title: "无内容人物过滤机制实现"
usage_scenario:
    - "实现人物/作者列表的筛选逻辑，排除无关联内容的条目"
    - "调整 Repository 层数据返回范围以控制 UI 展示"
    - "排查多页面人物列表不一致问题时定位过滤点"
keywords:
    - "人物过滤"
    - "预备役"
    - "Repository 层"
    - "DAO 查询"
---

## 任务描述
实现「无内容人物视为预备役」的过滤机制：在人物选择列表、默认选中状态及书架展示中，自动排除没有语录、诗歌或文章的预备役人物。

## 执行过程
```mermaid
graph TD
    A[需求:过滤无内容人物] --> B[在 PersonDao 新增 findContentPersonIds 查询]
    B --> C[SQL: SELECT personId FROM quote UNION SELECT personId FROM article]
    C --> D[修改 PersonSelectionRepository.getAllPersons 增加过滤逻辑]
    D --> E[同步修改 DailyRepository.selectedPool 确保每日卡片不展示]
    E --> F[验证 BookshelfViewModel/ProfileScreen 等入口是否统一调用 getAllPersons]
    F --> G[确认书签页天然有内容无需额外处理]
```

## 任务总结
成功实施预备役过滤：在 DAO 层通过 UNION 查询获取有内容人物 ID 集合，在 Repository 层统一过滤掉 styleKey 为编者且不在内容 ID 集合中的人物。该逻辑已覆盖人物选择页、书架页、我的页统计及每日推荐卡片，确保无内容人物仅存在于数据库中而不参与任何前端展示与交互。
