---
title: "免责声明页强制确认与拒退逻辑实现"
usage_scenario:
    - "App 启动页需要首次强制确认或协议签署场景"
    - "实现允许用户拒绝退出并保留下次提示的交互模式"
    - "扩写启动页引导文案并适配滚动布局"
keywords:
    - "免责声明"
    - "强制确认"
    - "拒绝退出"
    - "启动页"
    - "持久化"
---

## 任务描述
重构应用启动页免责声明：实现首次进入强制点击确认、提供拒绝退出权利、扩写辩证阅读理念文案。

## 执行过程
```mermaid
graph TD
    A[需求:免责声明需强制确认+拒绝退出+扩写文案] --> B[新建 DisclaimerStore 持久化同意状态]
    B --> C[在 QuoteApplication 注册单例]
    C --> D[修改 MainActivity 启动逻辑:判断 isAccepted]
    D --> E[已同意:原逻辑自动进入;未同意:显示双按钮]
    E --> F[重写 DisclaimerLoadingPage:标题固定+正文滚动]
    F --> G[添加争议人物阅读警示文案]
    G --> H[底部双按钮:同意并进入/拒绝退出]
    H --> I[拒绝回调调用 finishAffinity 直接退出]
    I --> J[编译验证通过]
```

## 任务总结
成功实现免责声明升级：新建 `DisclaimerStore` 持久化用户同意状态；首次启动显示「同意并进入」与「拒绝退出」双按钮，拒绝则直接退出且不记录状态；已同意用户自动跳过按钮。文案扩写五段，强调版本差异、历史背景、辩证批判阅读争议人物、反对教条主义及锻炼思想能力。布局采用标题固定、正文滚动适配小屏，风格保持项目规范。
