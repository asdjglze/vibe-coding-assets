---
title: "修复搜索工具链缺失链接及提示词引导"
usage_scenario:
    - "开发或调试 Agent 的多工具协作流程（如搜索后抓取正文）"
    - "排查 AI 无法访问详情页或无法深入阅读的原因"
    - "优化搜索结果的提示词策略以支持更复杂的推理"
keywords:
    - "Agent 工具链"
    - "search_web"
    - "fetch_page"
    - "提示词优化"
---

## 任务描述
修复 Agent 搜索工具链缺陷：解决 `search_web` 返回结果缺少链接导致 AI 无法调用 `fetch_page` 抓取正文的问题，并优化提示词以明确工具配合逻辑。

## 执行过程
```mermaid
graph TD
    A[用户反馈:搜索结果无摘要/正文] --> B[Grep 定位 agent.py 提示词与工具逻辑]
    B --> C[发现缺陷:search_web 返回不含 href 链接]
    C --> D[修改_search_tool:输出增加链接字段]
    D --> E[修改提示词:明确片段仅为线索需抓正文]
    E --> F[编写测试脚本:验证输出格式含链接]
    F --> G[确认修复:AI 可获取 URL 调用 fetch_page]
```

## 任务总结
成功修复搜索工具链：
1. **数据层**：`_search_tool` 返回格式由「标题 + 片段」升级为「标题 + 片段 + 链接」，确保 AI 拥有跳转 URL。
2. **指令层**：提示词明确 `search_web` 为线索收集，`fetch_page` 为正文核验，指导 AI 在片段不足时主动使用链接抓取权威源（如豆瓣）。
3. **效果**：闭环了「搜索->判断->抓取->分类」的 Agent 工作流，避免 AI 因信息缺失而误判。
