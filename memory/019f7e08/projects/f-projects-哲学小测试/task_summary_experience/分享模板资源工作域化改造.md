---
title: "分享模板资源工作域化改造"
usage_scenario:
    - "Android WebView 加载 assets 资源失败或需避免 APK 压缩影响"
    - "需要将静态资源（如字体、模板）解压到本地文件存储"
    - "实现资源文件的增量更新与缓存机制"
keywords:
    - "资源解压"
    - "工作域"
    - "WebView"
    - "assets"
    - "ShareTemplateExtractor"
---

## 任务描述
解决 Android WebView 中 openFd 读取 APK assets 失败的问题，实现分享模板资源（HTML/JS/CSS）从 assets 流改为解压到 files 目录（工作域）后读取。

## 执行过程
```mermaid
graph TD
    A[需求:资源全部解压到工作域] --> B[创建 ShareTemplateExtractor 工具类]
    B --> C[实现后台线程递归解压 assets/share_templates 到 files/share_templates]
    C --> D[在 QuoteApplication 启动时调用 extractAllAsync]
    D --> E[修改 ShareTemplateRepository: loadHtml/loadManifest 优先读文件]
    E --> F[修改 ShareCardWebView: shouldInterceptRequest 优先读文件]
    F --> G[修复路径逃逸防护与 MIME 类型获取]
```

## 任务总结
1. 新增 `ShareTemplateExtractor` 对象，实现增量解压与半文件防护。
2. 在 `QuoteApplication` 初始化阶段异步触发解压。
3. `ShareTemplateRepository` 与 `ShareCardWebView` 均改为「工作域文件优先，assets 兜底」策略。
4. 解决了因 APK 压缩导致 `openFd` 失败及缓存旧版 JS 的问题。
