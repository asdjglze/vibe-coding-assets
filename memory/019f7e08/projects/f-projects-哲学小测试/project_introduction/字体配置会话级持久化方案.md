---
title: "字体配置会话级持久化方案"
usage_scenario:
    - "设计用户个性化设置持久化机制时"
    - "实现前端字体选择与后端数据同步时"
    - "重构会话数据结构以支持新配置项"
keywords:
    - "fontConfig"
    - "session.json"
    - "会话绑定"
    - "持久化"
---

字体配置（fontConfig）需持久化到session.json中，与用户会话绑定，通过PUT接口保存、GET/latest-session接口返回，替代localStorage本地存储
