---
title: "禁止自动补全分类层级"
usage_scenario:
    - "设计或修改图书分类 Agent 的层级钻入逻辑时"
    - "排查分类路径生成异常或 AI 跳过层级导致错误时"
keywords:
    - "层级钻入"
    - "逐层确认"
    - "禁止自动补全"
---

图书分类 Agent 的层级钻入流程必须保留逐层确认机制，禁止程序自动补全中间层级（如 build_path）。AI 需要在每一层看到官方候选类目并做出选择，跳层会导致架构失效。
