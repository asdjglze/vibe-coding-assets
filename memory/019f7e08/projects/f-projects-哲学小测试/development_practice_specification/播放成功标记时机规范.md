---
title: "播放成功标记时机规范"
usage_scenario:
    - "修复自动跳歌无限循环 bug"
    - "设计媒体播放器的容错与恢复逻辑"
    - "优化播放器状态机流转"
keywords:
    - "播放成功标记"
    - "onSourceLoaded"
    - "自动跳过"
    - "状态机"
---

播放成功状态标记（_markPlaybackSuccess）不应在发出播放指令后立即调用，而应等待 onSourceLoaded 事件（ExoPlayer 真正就绪）后触发，避免错误重置连续失败计数导致自动跳过机制失效
