---
title: "动态数据禁止硬编码需存入数据库"
usage_scenario:
    - "重构包含大量硬编码数据的类或对象时"
    - "新增人物/样式等需要动态管理的数据时"
keywords:
    - "硬编码"
    - "Theme.kt"
    - "DAO查询"
    - "数据驱动"
---

项目规范要求：人物样式、头像映射等动态数据必须存储在数据库中，严禁在 Theme.kt 等代码文件中硬编码（如使用 val 或 mapOf），应通过 DAO 查询并在 Application 启动时初始化，以保持代码整洁并支持数据更新。
